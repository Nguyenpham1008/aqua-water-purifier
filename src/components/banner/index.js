import React from "react";

import bannerImg from "../../assests/banner.jpeg";

const Banner = () => (
  <div style={{ height: 500 }}>
    <img style={{ width: "100%", height: "auto" }} src={bannerImg} />
  </div>
);

export default Banner;
